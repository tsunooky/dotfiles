Faire un guide d'installation Arch

README




How to start on Arch?

changer la resolution :
xrandr --output eDP-1 --mode 3072x1920 --rate 120

Install a package:
sudo pacman -S <package>

Update every packages:
sudo pacman -Syu

Proberly remove a package on Arch:
sudo pacman -Rcns <package>


- Put vim shortcuts in NV Chad


Modify alacritty matugen colors

Rofi + powermenu

Keybinds helper (maybe rofi) : i3, vim, bashrc commands etc...

git config --global user.name "your_username"
git config --global user.email "github@mail.com"
ssh-keygen -C "github@mail.com" -t ed25519
xsel -b < ~/.ssh/id_ed25519.pub
copy and past the key on the clipboard to github SSH settings

then:
ssh -T git@github.com
On each repo for github to push with ssh keys:
git remote set-url origin git@github.com:your-repository.git

Regler problemes de compatibilites avec ecrans 4k : Xressources, polybar, rofi

